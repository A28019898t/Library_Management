#include <stdio.h>              // Thư viện chuẩn cho nhập/xuất dữ liệu

#include "../Headers/menu.h"    // File header chứa hàm hiển thị menu chính và menu con

int main() {                    // Hàm chính của chương trình
    mainMenu();                 // Gọi hàm hiển thị menu chính để bắt đầu hệ thống
    
    return 0;                   // Trả về 0 khi chương trình kết thúc thành công
}